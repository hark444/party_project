on:
  schedule:
    - cron: '0-59/6 * * * *' # Executes at 1 PM UTC every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Get all the env variables
        run: env
          
      - name: Getting ENV Var
        run: echo "The selected color is ${{ steps.random-color-generator.outputs.SELECTED_COLOR }}"
